export interface Education{
    institute: string,
    course: string,
    duration: string,
    score: string
}

export interface workExperience{
    role: string,
    company: string,
    duration: string,
    description: string[]
}

export interface skill{
    name: string;
    level: string;
    rating: number;
}